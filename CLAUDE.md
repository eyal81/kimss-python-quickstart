# Kimss Python quickstart — Claude Code

Read [AI_INTEGRATION.md](AI_INTEGRATION.md).

This repo demonstrates gateway proxy scripts. Prefer `example_02_openai_override.py` for the OpenAI `base_url` swap plus Agent-Id headers. Never use `KimssClient` for inference. Keys are `kimss_...`. Kill switch code is `agent_disabled`.
