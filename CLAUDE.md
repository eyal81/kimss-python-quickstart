# Kimss Python quickstart — Claude Code

Read [docs/KIMSS_ONBOARDING.md](docs/KIMSS_ONBOARDING.md).

This repo demonstrates three scripts only. Prefer `example_02_openai_override.py` for the 1-line `base_url` swap. Keys are `kimss_...`. Kill switch code is `agent_disabled`.
